# Optional — if unset, the frontend targets http://<page-host>:8000 automatically
# (works for localhost, LAN IP from a phone, etc). Only set this to override.
# VITE_API_BASE_URL=http://localhost:8000
